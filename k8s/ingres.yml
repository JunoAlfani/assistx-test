apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: assistx-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: devops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: assistx-ingress
                port:
                  number: 80
